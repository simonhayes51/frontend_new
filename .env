# API Configuration
VITE_API_URL=https://api.futhub.co.uk
VITE_API_BASE_URL=https://api.futhub.co.uk
# Environment Variables File
# Create this file in your project root as .env

# Development only (remove in production)
VITE_DEBUG=true

# Make sure the variable name starts with VITE_ 
# This is required for Vite to include it in the build
